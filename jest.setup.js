// jest.setup.js


require('dotenv').config({path: '.env.test' });
// jest.setup.js (or directly in your test files)


//integrational testing!!!
//require('dotenv').config(); // This will default to using '.env' that stores the real data to interact with external APIs.